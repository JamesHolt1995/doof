---

---

<div class="container">
	<div class="video-inner">
		<video src="./video_export.mp4" autoplay muted loop playsinline></video>
	</div>
</div>

<script>
	// import lottie from "lottie-web";

	// const el = document.querySelector(".animation");

	// lottie.loadAnimation({
	// 	container: el, // the dom element that will contain the animation
	// 	renderer: "svg",
	// 	loop: true,
	// 	autoplay: true,
	// 	path: "./animation.json", // the path to the animation json
	// });
</script>

<style lang="scss">
	.container {
		width: 100%;
		height: 100vh;
		display: flex;
		justify-content: center;
		align-items: center;
		.video-inner {
			position: relative;
			max-width: 1080px;
			width: 100%;
			&:after,
			&:before {
				content: "";
				position: absolute;
				left: 0;
				top: 0;
				width: 100%;
				height: 150px;
				background: linear-gradient(
					180deg,
					rgba(0, 0, 0, 1) 0%,
					rgba(0, 0, 0, 0) 100%
				);
			}
			&:before {
				right: 0;
				left: 0;
				bottom: 0;
				top: auto;
				transform: rotate(180deg);
			}
			video {
				width: 100%;
				margin: 0 auto;
			}
		}
	}

	@media screen and (min-width: 1080px) {
		.container {
			.video-inner {
				&:after,
				&:before {
					content: "";
					position: absolute;
					left: 0;
					top: 0;
					right: auto;
					bottom: 0;
					width: 150px;
					height: 100%;
					background: linear-gradient(
						90deg,
						rgba(0, 0, 0, 1) 0%,
						rgba(0, 0, 0, 0) 100%
					);
				}
				&:before {
					right: 0;
					left: auto;
					bottom: 0;
					top: 0;
					transform: rotate(180deg);
				}
			}
		}
	}
</style>
